import React from 'react';
import PropTypes from 'prop-types';

Chat.propTypes = {

};

function Chat(props) {
    return (
        <div>
            <h1>Chat</h1>
        </div>
    );
}

export default Chat;